# Changelog

## 0.1.0 — initial release

- `caipLightTheme` and `caipDarkTheme` Fluent UI React v9 themes
- `caipSemanticTokens` with named CAIP colour roles for light + dark
- `caipGradients` and `caipCssVariables` for Cloud, AI, and stroke gradients
- ESM + CJS builds with TypeScript declarations
